import React from 'react'

const Xpoints = ({value}) => {
    return (
        <div className="xpoints">
            <h2>X - Points</h2>
            <h2 className="val">{value}</h2>
        </div>
    )
}

export default Xpoints
