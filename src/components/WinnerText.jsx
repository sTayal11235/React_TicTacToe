import React from 'react'

const WinnerText = ({value}) => {
    return (
        <div className = "winner">
            <h2  className="wintxt">{value}</h2>
        </div>
    )
}

export default WinnerText